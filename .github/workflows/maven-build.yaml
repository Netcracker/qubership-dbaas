---
name: Maven build

on:
  push:
  pull_request:
    types: [ opened, synchronize, reopened ]

jobs:
  mvn:
    uses: netcracker/qubership-core-infra/.github/workflows/maven-build-with-sonar.yaml@81251ff6dcf6feecf760e02d09b6bc9cd8c99e53 # v2.0.0
    with:
      event-name: ${{ github.event_name }}
      actor: ${{ github.actor }}
      upload-artifact: true

  docker-build:
    needs: [mvn]
    uses: netcracker/qubership-core-infra/.github/workflows/docker-build.yaml@81251ff6dcf6feecf760e02d09b6bc9cd8c99e53 # v2.0.0
    with:
      dry-run: false
