---
name: Maven build

on:
  push:
  pull_request:
    types: [ opened, synchronize, reopened ]

jobs:
  mvn:
    uses: netcracker/qubership-core-infra/.github/workflows/generic-maven-build.yaml@c36c6cf6875af12c5708dcdbe088a887be55785c # v2.2.0
    with:
      upload-artifact: true
      java-version: 21
