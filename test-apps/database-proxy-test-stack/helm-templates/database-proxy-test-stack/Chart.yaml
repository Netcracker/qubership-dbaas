apiVersion: v2
name: database-proxy-test-stack
description: DBaaS Proxy and HAProxy fixture that puts a controllable proxy in the DBaaS control and data paths
type: application
version: 0.1.0
appVersion: "0.1.0"
