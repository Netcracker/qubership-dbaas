# This patch sets the manager's HTTP server bind address. The listener
# hosts the Prometheus /metrics endpoint.
- op: add
  path: /spec/template/spec/containers/0/args/0
  value: --http-bind-address=:8080
