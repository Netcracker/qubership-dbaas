---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: permanentbalancingrules.dbaas.netcracker.com
spec:
  group: dbaas.netcracker.com
  names:
    categories:
    - dbaas
    kind: PermanentBalancingRule
    listKind: PermanentBalancingRuleList
    plural: permanentbalancingrules
    shortNames:
    - dbpbr
    singular: permanentbalancingrule
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1
    schema:
      openAPIV3Schema:
        description: |-
          PermanentBalancingRule is the Schema for the permanentbalancingrules API.
          It declares a permanent namespace-level physical database placement rule.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of PermanentBalancingRule.
            properties:
              rules:
                description: rules is the set of permanent balancing rules managed
                  by this singleton CR.
                items:
                  description: PermanentBalancingRuleItem defines one permanent balancing
                    rule entry.
                  properties:
                    dbType:
                      description: |-
                        dbType is the database engine type this rule applies to, e.g. "postgresql" or "mongodb".
                        The Pattern rejects a whitespace-only value, which MinLength alone accepts.
                      minLength: 1
                      pattern: \S
                      type: string
                    namespaces:
                      description: namespaces is the list of namespaces where the
                        permanent rule applies.
                      items:
                        minLength: 1
                        pattern: \S
                        type: string
                      minItems: 1
                      type: array
                      x-kubernetes-list-type: set
                    physicalDatabaseId:
                      description: |-
                        physicalDatabaseId is the physical database identifier where new logical
                        databases of this type should be created. The operator validates this
                        value for shape only; a Succeeded status means dbaas-aggregator stored
                        the rule, not that this identifier was resolved to a registered physical
                        database.
                      minLength: 1
                      pattern: \S
                      type: string
                  required:
                  - dbType
                  - namespaces
                  - physicalDatabaseId
                  type: object
                minItems: 1
                type: array
                x-kubernetes-list-type: atomic
            required:
            - rules
            type: object
          status:
            description: status defines the observed state of PermanentBalancingRule.
            properties:
              appliedRules:
                description: |-
                  appliedRules are the permanent rule entries last successfully applied
                  to dbaas-aggregator. They are used to delete removed entries when spec
                  changes. They do not guarantee that referenced physical databases exist.
                items:
                  description: |-
                    PermanentBalancingRuleAppliedRule tracks one successfully applied permanent
                    rule entry for cleanup on update/delete.
                  properties:
                    dbType:
                      description: dbType is the database engine type last applied.
                      type: string
                    namespaces:
                      description: namespaces is the namespace set last applied for
                        dbType.
                      items:
                        type: string
                      type: array
                      x-kubernetes-list-type: set
                  required:
                  - dbType
                  - namespaces
                  type: object
                type: array
                x-kubernetes-list-type: atomic
              conditions:
                description: |-
                  conditions represent the current state of the resource.
                  Ready describes whether the current generation was successfully processed.
                  NamespaceBinding uses BindingRegistered on success, BindingBlocked while
                  deletion is deferred, BindingReleased after its protection finalizer is
                  removed, and OwnershipCheckError when listing blocking resources fails.
                  Stalled=True marks a permanent error that will not retry until the spec changes;
                  Stalled=False marks a transient error that the controller retries automatically.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              lastRequestId:
                description: |-
                  lastRequestId is the X-Request-Id of the most recent reconcile attempt
                  that wrote this status; a reconcile that leaves the status untouched (for
                  example a steady-state NamespaceBinding reconcile) keeps the previous
                  value. Use it to correlate operator logs with dbaas-aggregator logs when
                  investigating issues for a specific resource.
                type: string
              observedGeneration:
                description: |-
                  observedGeneration reflects the .metadata.generation that was last processed
                  by the controller. When the current generation differs from this value, the
                  controller clears all conditions and starts a fresh reconciliation cycle.
                  Zero (or less than metadata.generation) means the current generation has not
                  yet been fully reconciled — for example, the controller is in BackingOff
                  waiting for a transient error to clear.
                format: int64
                type: integer
              phase:
                description: |-
                  phase is a human-readable summary of the conditions below, provided so that
                  `kubectl get` can show one column. Do not automate against it — read
                  conditions instead. Not defaulted by the API server: status is owned by the
                  controller, which always sets phase alongside the conditions.
                type: string
            type: object
        required:
        - spec
        type: object
        x-kubernetes-validations:
        - message: PermanentBalancingRule name must be 'permanent-balancing-rules'
          rule: self.metadata.name == 'permanent-balancing-rules'
    served: true
    storage: true
    subresources:
      status: {}
