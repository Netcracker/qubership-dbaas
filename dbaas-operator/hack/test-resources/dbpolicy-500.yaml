apiVersion: dbaas.netcracker.com/v1alpha1
kind: DbPolicy
metadata:
  name: dp-500
spec:
  # microserviceName "svc-500" matches apply-rules.json → mock returns 500 CORE-DBAAS-2000
  # Expected result: Phase=BackingOff, Stalled=False, reason=AggregatorError (retrying)
  microserviceName: svc-500
  services:
    - name: other-service
      roles:
        - readonly
