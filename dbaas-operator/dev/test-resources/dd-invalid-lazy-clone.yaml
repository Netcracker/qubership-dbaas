# DatabaseDeclaration — controller pre-flight rejects lazy=true + approach=clone
# No aggregator call is made — the controller catches this cross-field constraint.
# Expected: Phase=InvalidConfiguration/InvalidSpec, Stalled=True (no requeue)
apiVersion: dbaas.netcracker.com/v1alpha1
kind: DatabaseDeclaration
metadata:
  name: dd-invalid-lazy-clone
  namespace: test-ns
spec:
  classifier:
      microserviceName: dd-svc-lazy
      scope: service
  type: postgresql
  lazy: true
  initialInstantiation:
    approach: clone
    sourceClassifier:
      microserviceName: dd-svc-lazy
      scope: service
