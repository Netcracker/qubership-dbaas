# InternalDatabase — aggregator returns HTTP 500
# apply-rules.json key: "idb-svc-500" → httpCode=500 CORE-DBAAS-2000
# Expected: Phase=BackingOff, Ready=False/AggregatorError, Stalled=False (retries)
apiVersion: dbaas.netcracker.com/v1
kind: InternalDatabase
metadata:
  name: idb-500-server-error
  namespace: test-ns
spec:
  classifier:
      microserviceName: idb-svc-500
      scope: service
  type: postgresql
