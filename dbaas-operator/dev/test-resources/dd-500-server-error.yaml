# DatabaseDeclaration — aggregator returns HTTP 500
# apply-rules.json key: "dd-svc-500" → httpCode=500 CORE-DBAAS-2000
# Expected: Phase=BackingOff, Ready=False/AggregatorError, Stalled=False (retries)
apiVersion: dbaas.netcracker.com/v1
kind: DatabaseDeclaration
metadata:
  name: dd-500-server-error
  namespace: test-ns
spec:
  classifier:
      microserviceName: dd-svc-500
      scope: service
  type: postgresql
