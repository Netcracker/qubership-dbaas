apiVersion: dbaas.netcracker.com/v1
kind: DbPolicy
metadata:
  name: dp-400
  namespace: test-ns
spec:
  # microserviceName "svc-400" matches apply-rules.json → mock returns 400 CORE-DBAAS-4036
  # Expected result: Phase=InvalidConfiguration, Stalled=True, reason=AggregatorRejected
  microserviceName: svc-400
  services:
    - name: other-service
      roles:
        - admin
