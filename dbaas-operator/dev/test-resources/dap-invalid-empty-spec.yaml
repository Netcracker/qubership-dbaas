apiVersion: dbaas.netcracker.com/v1
kind: DatabaseAccessPolicy
metadata:
  name: dap-invalid-empty-spec
  namespace: test-ns
spec:
  operatorNamespace: dbaas-system
  # Neither services, policy, nor disableGlobalPermissions are set —
  # CRD admission rejects this CR with HTTP 422 (CEL cross-field rule).
  # Expected: kubectl apply returns an error; the object is never stored.
  microserviceName: svc-invalid-empty-spec
